# 设计说明与待决语义

本文件记录从原业务项目抽离时**发现但未擅自修改**的语义分歧。

原则：抽离阶段以「不改变既有解析行为」为最高优先级。以下每一条都保留现状，
待讨论后再通过带版本号的行为变更（并更新测试）落地。

---

## 1. 「本月 / 本季度 / 今年」返回完整周期还是截至今天？

**现状不一致。** 以 `today = 2026-05-24` 实测：

| 输入 | 现状返回 | 语义 | `range_type` |
| --- | --- | --- | --- |
| 本月 | `2026-05-01 ~ 2026-05-24` | 截至今天（MTD） | `range` |
| 今年 | `2026-01-01 ~ 2026-05-24` | 截至今天（YTD） | `range` |
| 本周 | `2026-05-18 ~ 2026-05-24` | 完整自然周（含未来） | `range` |
| 本季度 | `2026-04-01 ~ 2026-06-30` | 完整自然季度（含未来） | `quarter` |

以 `today = 2026-05-20`（周三）复测，确认差异**不是**基准日巧合：

| 输入 | 返回 | 说明 |
| --- | --- | --- |
| 本周 | `2026-05-18 ~ 2026-05-24` | 末日 05-24 在今天之后 → 完整周 |
| 本月 | `2026-05-01 ~ 2026-05-20` | 截断到今天 |
| 今年 | `2026-01-01 ~ 2026-05-20` | 截断到今天 |
| 本季度 | `2026-04-01 ~ 2026-06-30` | 完整季度 |

**分歧点**：同为"本 X"，**本月/今年截断到今天，本周/本季度返回完整周期**，
口径不统一。对统计类调用方，"本周"/"本季度"会把尚未发生的日期算进区间。

**两种候选口径**：
- A. 全部统一为「截至今天」——符合"当前进行中的周期只有过去部分有数据"的直觉；
  但"本季度目标"这类前瞻语义会被破坏。
- B. 全部统一为「完整自然周期」——语义纯粹；由调用方自行与今天取交集。

**待决**：选 A 还是 B；或保留差异但在文档中明确写死。
另一可行方案：新增参数 `clamp_to_today: bool`，由调用方显式声明，默认维持现状。

---

## 2. 「最近 N 月」是滑动月份还是完整自然月？

**现状：滑动（按天回推）。** `today = 2026-05-24`：

- `最近3个月` / `近3个月` → `2026-02-24 ~ 2026-05-24`

即 `_months_ago()` 取"N 个月前的同一天"，得到跨自然月边界的滑动窗口。

代码中另有 `_n_months_before_range()`（返回 N 个月前的**完整自然月**
`1 号 ~ 月末`），但当前 `最近N月` 分支并未使用它，仅供其它分支调用。
两套口径并存于同一文件，容易误用。

**分歧点**：财务/经营统计通常期望"完整自然月"（如 3 月、4 月、5 月三个整月），
而现状给出的是 02-24~05-24 这种跨月区间，与月度报表无法对齐。

**待决**：是否为「最近N月」提供自然月口径（或增加参数切换）。
在决策前**不改动**现有滑动行为。

---

## 3. 端点是否包含？

**现状：`DateRange.includes_end` 恒为 `True`**，全部解析分支都未把它设成 `False`。
即所有区间都是闭区间 `[start, end]`，对应 SQL `BETWEEN`。

同时 `start` / `end` 都是 `YYYY-MM-DD` 日期字符串，**不含时分秒**。

**分歧点**：若调用方的数据是带时间戳的 `datetime`，用
`ts BETWEEN '2026-05-01' AND '2026-05-24'` 会漏掉 `2026-05-24` 当天
00:00 之后的记录（末日被当作 00:00:00）。此时正确写法是半开区间
`ts >= start AND ts < end + 1天`。

**待决**：
- `includes_end` 字段目前是恒真的死字段，是删掉、还是真正实现半开区间支持？
- 是否提供一个显式的 `end_exclusive` 属性（`end + 1 天`）给时间戳型数据源？

在决策前保留字段与恒真行为。

---

## 4. 两位年份的解释规则

**现状：一律补 `20` 前缀。** 实测：

- `25年` → `2025-01-01 ~ 2025-12-31` ✅ 符合预期
- `99年` → **`2099-01-01 ~ 2099-12-31`** ⚠️ 多数中文语境下"99年"指 1999 年

**分歧点**：无世纪推断逻辑，`YY` 直接映射为 `20YY`。

**常见替代规则**：
- POSIX/strptime 式滑动窗口：`69~99 → 19xx`，`00~68 → 20xx`。
- 以当前年份为中心的 ±50 年窗口。

**待决**：是否引入世纪推断，以及具体阈值。
考虑到业务数据多为近年，现状在实际使用中很少出错，故**暂不修改**。

---

## 5. `DateRange.range_type` 中的 `'yoy'` / `'mom'` 已不可达

**现状：死取值，保留不删。**

`DateRange.range_type` 的历史取值里包含 `'yoy'`（同比）和 `'mom'`（环比），
来自原业务项目「用一个 DateRange 兼表比较语义」的旧设计。

自 0.2.0 起，同比/环比改由独立的 `ComparisonRange` 表达
（`current` + `comparison` 两个区间 + `comparison_type`），
原因是**一个 DateRange 无法同时表达本期和对比期**，
而把对比期塞进 `point` / `boundary` 会污染这两个字段的既有语义。

经检查，当前解析链中**没有任何分支**会把 `range_type` 设成 `'yoy'` 或 `'mom'`，
这两个取值已经不可达。

**待决**：是否移除这两个取值。
本次**刻意不删**——外部调用方可能存在
`if r.range_type == 'yoy'` 这类分支，贸然删除属于破坏性变更。
建议在下一个 major 版本中随其它不兼容改动一并清理。

---

## 6. 半年范围暂不支持跨年份

`H1到H2`、`2025H1到2025H2` 已支持；但 `2025H2到2026H1` **刻意不支持**。

原因：跨年份时需要额外校验起止顺序（避免 `2026H1到2025H2` 产出反向区间），
且需要补齐相应测试。在这些校验和测试补全前，返回「不识别」比返回一个
可能错误的区间更安全。

**待决**：补全顺序校验与测试后开放跨年份半年范围。

---

## 7. 同比 / 环比的口径选择

以下是本次实现时做出的判断，记录备查：

- **「环比」统一建模为 `previous_period`**，而不是一律 month-over-month。
  季度的环比是上一季度，半年的环比是上一个半年。若强行都叫 "mom"，
  `Q2环比` 的语义会说不通。
- **进行中的周期用「相同已过天数」对齐**。`本月环比`（today=8-09）比较
  `8-01~8-09` 与 `7-01~7-09`，而不是与完整的 7 月比——否则 9 天对 31 天，
  比值没有意义。
- **完整周期用各自的真实月末**。`上个月环比` 比较 7 月（31 天）与 6 月（30 天），
  不做天数对齐，因为两者都是完整周期。
- **单独出现的「同比」默认为 YTD 同比**（今年至今 vs 去年同期），
  单独出现的「环比」默认为本月至今 vs 上月同期天数。
  这是经营分析中最常见的默认口径，但确实是一种**猜测**。
  **待决**：是否应改为返回 `None` 要求调用方明确指定周期。

  但「默认周期」**仅**在比较词独立出现时启用（即剥离比较词后剩余文本为空或只剩
  无语义标点/询问后缀，如「同比」「去年同期」「同比是多少」「去年同期怎么样」）。
  若剥离比较词后**仍存在实质性的时间表达但无法解析**（如「前阵子同比」
  「第五季度同比」「H3环比」「不支持的时间环比」），**必须返回 `None`**，
  不得为这类表达编造默认周期。
- **「同比」与「环比」同时出现时返回 `None`**（如「同比环比都要」「本月同比和环比」
  「Q2同比、环比」「同比及环比」）。

  > 2026-08 修复项：旧逻辑在此处「默认取同比」，但这会**丢失用户意图**
  > （用户明确要两种比较，却只拿到一种）。由于 `extract_comparison_range` 目前
  > 单次调用只返回一组 `ComparisonRange`，无法同时承载两种比较类型，
  > 因此改为**返回 `None`**，把澄清权交还给调用方，而不是擅自选一个。
  >
  > 未来若需要支持一次返回多组比较，可另行设计**列表型 API**
  > （如返回 `list[ComparisonRange]`），**本次不扩展返回类型**。
  > 注意：`DateRange.range_type` 中的 `'yoy'` / `'mom'` 历史取值与此无关，
  > 仍按 §5 保留不删。

---

## 8. 比较词触发器收紧（2026-08 修复）

- **环比触发器收紧**：环比**只**由「环比 / 较上期 / 比上期」触发。
  旧实现中包含单独的「环」，导致「环境数据 / 环节分析 / 循环统计 / 环岛记录」
  被误判为环比。已将单独的「环」从 `_MOM_PAT` 与 `_STRIP_PAT` 中移除，
  并同步从 `date_parser` 的比较词保护名单中移除（见下方第 9 节）。
- **同比触发器**：仅由「同比 / 去年同期 / 上年同期 / 同期」触发，不含单独的「同」，
  以免「同期生」「同比例」被误判。
- **`extract_date_range_v2` 的比较词保护**（新增，2026-08）：若输入含明确的比较表达
  （同比 / 环比 / 去年同期 / 上年同期 / 较上期 / 比上期），主入口**不再按普通 DateRange
  解析**，返回空 `DateRange`（`recognition_status='phrase_not_supported'`、
  `range_type='unknown'`、`label='比较表达请使用 extract_comparison_range'`）。
  该保护**不会**影响 `extract_comparison_range` 的正常功能：它先剥离比较词再调用
  `extract_date_range_v2`，传入的是剥离后的纯净时间短语（如「本月同比」→「本月」）。
  保护名单刻意不含单独的「同」「环」，因此「同期生」「环境数据」这类表达不会被误伤。

### 8.1 比较词边界二次收紧（2026-08 第二轮）

上一轮用「剩余文本是否含时间词」来兜底默认周期，仍过宽——「同期生」「同比例」等
普通词会被误判为默认同比。本轮改用**两道边界**：

1. **比较词检测层负向前瞻**：
   - 同比：`同比(?!例)`、`同期(?!生|刊|声)` —— 排除「同比例」「同期生/同期刊/同期声」。
   - 环比：`环比(?!例)` —— 排除「环比例」（「环比较」因不含连续「环比」子串天然不命中）；
     单独出现的「环比率」仍按环比识别。
   - `_STRIP_PAT` 与 `date_parser._COMPARISON_PHRASE_PAT` 同步收紧，避免误剥离/误拦截。
2. **独立默认表达严格白名单** `_STANDALONE_DEFAULT_PAT`（首尾锚定）：
   仅当原始输入精确匹配 `^（有限前缀）?（去年同期|上年同期|同比|环比|同期）（率）?（有限后缀）?（标点）?$`
   时才采用默认周期。允许的前缀/后缀为明确短清单（看/查询/帮我查/请查询… 与
   是多少/情况/数据/结果…），**任意其它夹带文本都不匹配**，从而把
   「同期生」「同比例」「环比例」「前阵子同比」「第五季度同比」「H3环比」等一律挡在默认周期之外。

注意：白名单是第二道边界，负向前瞻是第一道；二者互补，不能只靠其中一道。


---

## 8. 其它已知的口径记录（非分歧，仅备忘）

- `近半年` = `today - 183 天`；`近一年` = `today - 365 天`。
  按固定天数而非日历月/年回推，不考虑闰年。
  **注意与 `H1`/`H2` 区分**：`近半年` 是滑动 183 天，
  `H1`/`H2` 是自然半年（1-1~6-30 / 7-1~12-31，`granularity='half_year'`）。
  两者语义不同，`近半年` 的行为在本次新增 H1/H2 时保持原样未变。
- `近N天` 含今天（`today - (N-1) 天`）；
  而 `近N周` **不做** 类似的减一（`today - N*7 天`），两者边界处理不一致。
- `近N年` 按 `today.replace(year=...)` 回推，闰年 2-29 回退到 28 日。
- `几` 在中文数字解析中默认当作 `2`（如"这几年"→"近2年"）。
