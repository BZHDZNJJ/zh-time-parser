# 参与贡献

感谢你愿意改进 zh-time-parser。项目欢迎问题报告、解析规则建议、文档修正和代码贡献。

参与社区讨论与贡献即表示你同意遵守 [社区行为准则](CODE_OF_CONDUCT.md)。

## 提交问题

报告解析问题时，请尽量提供：

- 原始中文表达；
- 注入的 `today`、时区和 `week_start`；
- 实际结果与期望结果；
- Python 和 zh-time-parser 版本；
- 可公开的最小复现代码。

请勿提交客户名称、业务数据、密钥、内部地址或其他敏感信息。涉及安全问题时，
不要公开披露利用细节，应通过仓库维护者提供的私密联系方式报告。

## 本地开发

项目支持 Python 3.8–3.12。克隆仓库后安装开发依赖：

```bash
python -m pip install -e ".[dev]"
```

提交前运行完整检查：

```bash
python -m ruff check src tests
python -m mypy src/zh_time_parser
python -m pytest
python -m build --no-isolation
```

`pytest` 默认启用分支覆盖率检查，项目门禁为 85%。新增功能和缺陷修复都应包含测试。

## 设计与兼容性

`DateRange` 是项目的核心兼容接口。新增时间语义应优先采用独立模型和公开入口，
避免让 `extract_date_range_v2()` 因输入不同而返回不同类型。

贡献解析规则时请遵循以下原则：

- 能识别不等于必须猜测；模糊表达应保留模糊状态；
- 月、季度和年份偏移使用真实日历，不用固定 30/365 天近似；
- 不静默吞掉编程错误，也不把非法日期降级成另一个日期；
- 新规则应避免抢先匹配已有表达；
- 1.x 已冻结的日期口径以 [DESIGN.md](DESIGN.md) 为准；破坏性语义调整需要新的主版本。

## 测试约定

- `tests/test_date_parse.py`：DateRange 完整规则契约；
- `tests/test_date_parser.py`：不同锚点下的端到端补充冒烟；
- 独立模型或解析器使用对应的专项测试文件；
- 跨模块重复使用的时间锚点放入 `tests/conftest.py`；
- 边界缺陷应同时包含失败输入和相邻正常输入，防止修复造成误伤；
- 新增测试函数应添加参数与返回值类型标注。

## Pull Request

建议一个 PR 只解决一个主题。提交前请确认：

- 说明了改动原因和用户可见行为；
- 添加或更新了相应测试；
- Ruff、mypy、pytest 和构建均通过；
- 没有降低覆盖率门禁或加入宽泛异常捕获；
- 没有提交生成目录、敏感信息或无关格式化改动；
- 用户可见功能已同步更新 README、DESIGN 或 CHANGELOG。

维护者可能要求补充反例、兼容性说明或拆分过大的变更。提交 PR 即代表你同意项目
在 MIT License 下发布你的贡献。
